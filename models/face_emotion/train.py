"""Training template for face emotion recognition model."""

from __future__ import annotations


def train_face_emotion_model(dataset_dir: str, output_dir: str) -> None:
    """Train visual emotion recognition model.

    Next steps:
    1. Build image dataset + augmentations.
    2. Train CNN/ViT-based classifier.
    3. Evaluate and export best checkpoint.
    """
    # TODO: Implement training logic.
    print(f"[TODO] Train face model from {dataset_dir} and save to {output_dir}")
